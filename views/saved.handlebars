<main role="main">
    <section>
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <ul class="story-list">
                        {{#each stories}}
                        {{> saved/saved-stories}}
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </section>
</main>